<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Note App</title>
  </head>
  <body>
    <div
      class="main w-full min-h-screen bg-gradient-to-br from-gray-900 via-zinc-800 to-gray-900 flex flex-col justify-center items-center p-8"
    >
      <div class="max-w-md w-full bg-zinc-800 p-8 rounded-xl shadow-xl">
        <h1 class="text-3xl font-semibold text-center text-white mb-6">
          Change Task Name
        </h1>
        <form action="/edits" method="POST" class="space-y-6">
          <!-- Old Task Title Input -->
          <div>
            <label for="oldTaskTitle" class="block text-gray-400 mb-2"
              >Old Task Title</label
            >
            <input
              id="oldTaskTitle"
              type="text"
              placeholder="Old Task Title"
              name="OldTitle"
              value="<%= filename %>"
              readonly
              class="block w-full p-4 rounded-md bg-zinc-700 text-gray-200 border border-zinc-600 focus:ring-2 focus:ring-blue-500 focus:outline-none transition-shadow shadow-sm"
            />
          </div>

          <!-- New Task Title Input -->
          <div>
            <label for="newTaskTitle" class="block text-gray-400 mb-2"
              >New Task Title</label
            >
            <input
              id="newTaskTitle"
              type="text"
              placeholder="New Task Title"
              name="NewTitle"
              class="block w-full p-4 rounded-md bg-zinc-700 text-gray-200 border border-zinc-600 focus:ring-2 focus:ring-blue-500 focus:outline-none transition-shadow shadow-sm"
            />
          </div>

          <!-- Submit Button -->
          <button
            type="submit"
            class="w-full p-4 rounded-md bg-yellow-600 text-white font-medium hover:bg-yellow-700 focus:ring-2 focus:ring-blue-400 focus:ring-offset-2 focus:ring-offset-zinc-700 transition-all"
          >
            Change FileName
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
